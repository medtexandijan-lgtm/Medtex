## Frontend

Bu papkada UI qismi joylashgan:

- `templates/` - Django render qiladigan HTML template'lar
- `static/` - frontend assetlar (`css`, keyinchalik `js`, `img`)

Backend endi template va static fayllarni shu papkadan o'qiydi.
