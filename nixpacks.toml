[phases.setup]
nixPkgs = ["python314"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = [
    "python manage.py collectstatic --noinput",
    "python manage.py migrate --noinput"
]

[start]
cmd = "gunicorn medical_crm.wsgi --bind 0.0.0.0:$PORT --log-file -"
